<Window x:Class="VideoTracker.AmazonVideoSeriesForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Amazon Video On-Demand Series" SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Series Name:"/>
        <TextBox x:Name="titleBox" Grid.Row="0" Grid.Column="1" MaxWidth="200" MinWidth="200"/>
        <Label Grid.Row="1" Grid.Column="0" Content="Keywords or Product URL:"/>
        <TextBox x:Name="keywordBox" Grid.Row="1" Grid.Column="1" MaxWidth="200" MinWidth="200"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Grid.Column="1" Content="OK" Padding="5,2" Margin="20,2" IsDefault="True" Click="OKButton_Click"></Button>
            <Button Grid.Column="2" Content="Cancel" Padding="5,2" Margin="20,2" IsCancel="True"></Button>
        </StackPanel>
    </Grid>
</Window>
